server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # react-router's BrowserRouter needs every unknown path to fall back
    # to index.html — otherwise refreshing /chat, /documents, etc. 404s.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
